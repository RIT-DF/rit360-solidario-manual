# Preview local do manual (o GitHub Pages builda sozinho — este Gemfile é só
# para rodar `bundle exec jekyll serve` na sua máquina, se quiser conferir antes
# de publicar). Publicação continua sendo `bin/publish-manual.sh`.
source "https://rubygems.org"

gem "jekyll", "~> 4.3"
gem "just-the-docs", "~> 0.8"

group :jekyll_plugins do
  gem "jekyll-seo-tag"
end
